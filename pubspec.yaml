name: mg_common_game
description: Monthly Games 공통 게임 엔진 모듈 - 통합 UI 컴포넌트, 게임 시스템, 수익화 모듈
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.14.0
  firebase_core: ^2.32.0
  firebase_analytics: ^10.10.7
  firebase_crashlytics: ^3.5.7
  firebase_remote_config: ^4.4.7
  # cloud_firestore: ^4.17.5
  # firebase_auth: ^4.16.0
  get_it: ^9.2.0
  injectable: ^2.3.2
  freezed_annotation: ^3.1.0
  json_annotation: ^4.8.1
  shared_preferences: ^2.2.2
  intl: ^0.20.2
  flame_spine: ^0.3.0+1
  flame_audio: ^2.11.12
  vibration: ^1.8.4
  google_mobile_ads: ^5.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.7
  freezed: ^3.2.3
  json_serializable: ^6.7.1
  injectable_generator: ^2.4.1
  mocktail: ^1.0.1

flutter:
  uses-material-design: true
