import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors (Dark Tech / Neon)
  static const Color primary = Color(0xFF00E5FF); // Neon Cyan
  static const Color primaryVariant = Color(0xFF00B8D4);
  static const Color secondary = Color(0xFFD500F9); // Neon Purple
  static const Color secondaryVariant = Color(0xFFAA00FF);

  // Backgrounds (Deep Dark)
  static const Color background = Color(0xFF050505); // Almost Black
  static const Color surface = Color(0xFF1E1E1E); // Fallback solid
  static const Color surfaceGlass = Color(0xCC1A1A1A); // Translucent
  static const Color surfaceLight = Color(0xFF2C2C2C); // Hover states
  static const Color panel = Color(0xFF121212);

  // Elements (For Game Logic)
  static const Color fire = Color(0xFFFF2B2B); // Neon Red
  static const Color water = Color(0xFF2B88FF); // Neon Blue
  static const Color earth = Color(0xFF00E676); // Neon Green

  // Text
  static const Color textHighEmphasis = Color(0xFFFFFFFF);
  static const Color textMediumEmphasis = Color(0xCCFFFFFF); // 80%
  static const Color textDisabled = Color(0x62FFFFFF); // 38%

  // Semantic
  static const Color error = Color(0xFFFF1744);
  static const Color success = Color(0xFF00E676);
  static const Color warning = Color(0xFFFFEA00);
  static const Color info = Color(0xFF2979FF);

  // Border / Dividers
  static const Color border = Color(0xFF333333);
}
